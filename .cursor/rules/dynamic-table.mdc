---
description: DynamicTable sticky toolbar, DataTableFrame, TablePageViewport
globs: src/components/shared/table/**/*,src/features/**/components/tables/**/*,src/features/**/components/pages/**,src/app/(app)/**/page.tsx
alwaysApply: false
---

# DynamicTable

Use the shared `DynamicTable` for feature lists. Do not invent a parallel DataTable or bury row actions inside `ColumnConfig.format`.

## Layout

| Concern | Path |
|---------|------|
| Frame (sticky toolbar) | `src/components/shared/table/data-table-frame.tsx` |
| Table | `src/components/shared/table/dynamic-table.tsx` |
| Loading skeleton | `src/components/shared/table/table-skeleton.tsx` (`TableSkeleton`) |
| Barrel | `@/components/shared/table` |
| Feature columns | `src/features/<feature>/components/tables/*-table-columns.tsx` |
| List pages | `src/features/<feature>/components/pages/*` |

## Sticky toolbar (required)

Toolbar / search stay fixed; **only** the body scrolls. Pagination stays in the footer.
Column headers are sticky inside the body so sort controls stay reachable while scrolling rows.

1. Route: wrap with `<TablePageViewport>` (fills the locked AppShell content slot)
2. Table: use `<DynamicTable>` (uses `DataTableFrame` internally) or empty via `<DataTableFrame>`
3. Loading: use `<TableSkeleton toolbarActions={disabledCreate} />` — keep toolbar chrome; never text-only “Loading…”
4. Parent of `DynamicTable` / `TableSkeleton` must be in a `h-full` / `min-h-0` / `flex-1` / `overflow-hidden` chain
5. **Never** put `DynamicTable` inside a page-level `overflow-y-auto` that scrolls the toolbar away — only `.table-surface` scrolls
6. Multi-section pages: each section is `flex-1 min-h-0 overflow-hidden` with the table filling that section
7. Paginate with `PAGE_SIZE` from `@/components/shared/table` (default; override `pageSize` only when needed). Footer stays pinned: results summary left, page controls right (omit controls when a single page)

```tsx
// ✅ route
import { TablePageViewport } from "@/components/shared/table"

export default function Page() {
  const page = useXListPage()
  return (
    <TablePageViewport>
      <XListPage {...page} />
    </TablePageViewport>
  )
}
```

```tsx
// ❌ page scrolls the whole table including search
<div className="overflow-y-auto">
  <DynamicTable … />
</div>
```

## API

```ts
type DynamicTableProps = {
  data: Record<string, unknown>[]
  columns?: ColumnConfig[]
  pageSize?: number
  searchable?: boolean
  sortable?: boolean
  filterable?: boolean
  groupable?: boolean
  toolbarActions?: ReactNode
  rowActions?: (ctx: { row: Record<string, unknown> }) => ReactNode
}
```

- `data` is flattened rows (`toXTableRow`), not rich domain objects
- Map `row.id` back to the domain entity for Edit/Delete handlers
- `toolbarActions` → Create (and similar); `rowActions` → use `RowActionsMenu` + `RowActionItem` (single ⋯ trigger, actions in a popover with tooltips). Do not stack bare icon buttons in the actions column.
- Toolbar: search + `toolbarActions` on one row; Filters / Group by / Clear live in a toolbar `RowActionsMenu` (`⋯`), not as separate always-visible buttons.
- Actions column is automatic when `rowActions` is set; skip group header rows
- Never use key `__ed` in column configs / search fields

```tsx
rowActions={({ row }) => {
  const item = items.find((x) => x.id === row.id)
  if (!item) return null
  return (
    <RowActionsMenu label={`Actions for ${item.name}`}>
      <RowActionItem
        label="Edit"
        icon={<Pencil className="h-4 w-4" />}
        onClick={() => onEdit(item)}
      />
      <RowActionItem
        label="Delete"
        icon={<Trash2 className="h-4 w-4" />}
        destructive
        onClick={() => void onDelete(item)}
      />
    </RowActionsMenu>
  )
}}
```

## Feature columns file

Export `xTableColumns: ColumnConfig[]` and `toXTableRow(entity)`. Prefer displaying joined names (`departmentName`) over raw UUIDs.

See `.docs/components/tables.md` and list-page CRUD docs for wiring.
